# macOS noise
.DS_Store

# Claude Code workspace
.claude/
CLAUDE.md

# Local dev archives (kept outside the repo, see .gitignore)
research/
marketing/
reference/
dist/
*.zip

# PCP audit artifacts
PLUGIN-CHECK-FINDINGS.md
PLUGIN-CHECK-BASELINE.md

# Repo metadata
.distignore
.gitignore
.git/
.editorconfig

# IDE / editor
.vscode/
.idea/
*.code-workspace

# Build / dependency artifacts
node_modules/
vendor/
composer.lock
package-lock.json

# Tests
tests/
phpunit.xml
phpcs.xml
